Spring & Gradle configuration of Activiti REST using postgresql

database properties can be configured from
/resources/db.properties

Application assumes an empty database named Activiti is created and the user has admin access to database.

to run application, execute
<pre><code>
gradlew tRW -Duser.language=en -Duser.region=US
</code></pre>
